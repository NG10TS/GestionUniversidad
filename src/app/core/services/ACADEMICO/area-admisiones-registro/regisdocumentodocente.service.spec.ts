/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RegisdocumentodocenteService } from './regisdocumentodocente.service';

describe('Service: Regisdocumentodocente', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RegisdocumentodocenteService]
    });
  });

  it('should ...', inject([RegisdocumentodocenteService], (service: RegisdocumentodocenteService) => {
    expect(service).toBeTruthy();
  }));
});
